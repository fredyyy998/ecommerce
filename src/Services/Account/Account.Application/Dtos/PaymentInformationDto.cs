namespace Account.Application.Dtos;

public record PaymentInformationDto(
    AddressDto Address);
    // TODO concrete implementation is missing, this has to be extended with the concrete implementation of payment